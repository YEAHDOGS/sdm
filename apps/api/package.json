{
  "name": "@help/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p .",
    "db:migrate:local": "wrangler d1 migrations apply help-db --local",
    "db:migrate": "wrangler d1 migrations apply help-db --remote"
  },
  "dependencies": {
    "@help/shared": "*",
    "hono": "^4.5.9"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.0"
  }
}
